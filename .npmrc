# npm 12+ defaults allow-git=none.
# This package pins @fabric/core from GitHub. Downstream apps that install
# @fabric/http (and Hub) from git also nest-prepare core/http commit SHAs —
# `allow-git=root` refuses those fetches. Use `all` for the Fabric git stack.
allow-git=all
