'use strict';

// TODO: toggle for "web" target?  weback...
// const Server = require('./server');
const Client = require('./client');
const App = require('./app');
const SPA = require('./spa');

module.exports = {
  // Server: Server,
  Client: Client,
  App: App,
  SPA: SPA
};
