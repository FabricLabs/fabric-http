# Fabric lettermark

Serif lowercase **f** (Arvo Bold) in white on rich royal purple `#4C1D95`.

Canonical generator: `npm run make:icons` (`scripts/render-fabric-icon.js`).
Do not redraw this mark in downstream apps — copy these files.
